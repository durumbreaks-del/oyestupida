# oyestupida

A small interactive web experience built with HTML/JS/CSS. Includes a draggable frame, sound effects, and animated UI elements.

## Development

- Open `index.html` in a browser to run locally.

## Deployment

Connect this repository to Vercel and deploy from the `main` branch.
